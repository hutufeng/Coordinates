/**
 * Supabase 配置文件
 *
 * 使用步骤：
 * 1. 访问 https://supabase.com → 免费注册并创建项目
 * 2. 进入项目「设置 → API」页面
 * 3. 复制「项目地址（Project URL）」和「匿名公钥（anon public key）」
 * 4. 将下方两项替换为你自己的值后保存
 */
window.SUPABASE_CONFIG = {
  url: 'https://YOUR_PROJECT.supabase.co',   // ← 替换为项目地址
  anonKey: 'YOUR_ANON_KEY'                   // ← 替换为 anon public key
};
